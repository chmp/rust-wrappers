# `python_wasmer` - using the C-ABI library as WebAssembly module in Python using Wasmer 

To execute this example, navigate to the project root and run 

```bash
python make.py python_wasm
```

